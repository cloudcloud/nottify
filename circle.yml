machine:
  timezone:
    Australia/Sydney
  environment:
    CIRCLE_ENV: test
  services:
    - docker

dependencies:
  pre:
    - CGO_ENABLED=0 GOOS=linux go build -a -tags netgo -ldflags '-w' -o nott .

general:
  artifacts:
    - "./nott" # the built executable
